apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
  namespace: online-store
data:
  SPRING_PROFILES_ACTIVE: "docker"
  DB_HOST: "postgres-auth"
  DB_PORT: "5432"
  DB_NAME: "online_store_auth"
  ACTUATOR_ENDPOINTS: "health,info"
