apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: online-store
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - port: 8081
      targetPort: 8081
      name: http
    - port: 9090
      targetPort: 9090
      name: grpc
